import React, { useStatepermisions } from "react";

const username_last_first_List = ({ nicknames_frequency }) => (
  <ul>
    {names.map((nicknames_frequency, index) => (
      <li key={index}>{nicknames_frequency}</li>
    ))}
  </ul>
);

export default App;